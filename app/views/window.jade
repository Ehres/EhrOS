.window.window-default(ng-draggable, ng-resizable, ng-model="window")
    .transparent-bg
    .window-header
        button.close(type="button", ng-click="close()") &times;
        h4 {{window.title}}

    .window-body
        | {{window.content}}

    .window-footer